flask==3.0.*
werkzeug==3.0.*
# Repo-specific dependencies:
ultralytics>=8.0.200
opencv-python-headless>=4.8.0
numpy>=1.24.0
lap>=0.4.0  # Required for ByteTrack tracker in YOLO
# Note: torch and torchvision are installed separately in Dockerfile
# with CPU-only index to reduce image size.
# If GPU support is needed, remove the --index-url flag in Dockerfile.
