flask==3.0.*
werkzeug==3.0.*
# YOLO11n dependencies (requires ultralytics >=8.3.0 for YOLO11 support)
# Note: PyTorch is installed in the Dockerfile (cpu or gpu variant via DEVICE build arg)
ultralytics>=8.3.0
opencv-python-headless>=4.8.0
numpy>=1.24.0
