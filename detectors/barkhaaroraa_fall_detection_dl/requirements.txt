flask==3.0.*
werkzeug==3.0.*
# Repo-specific dependencies (fall detection only — face_recognition excluded):
mediapipe==0.10.9
opencv-python-headless>=4.8.0
numpy>=1.24.0
# Note: face_recognition and dlib are NOT included.
# They are only used for the supplementary facial recognition feature
# which is not part of the fall detection adapter.
